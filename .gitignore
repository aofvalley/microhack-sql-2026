# Build artifacts / OS
.DS_Store
Thumbs.db
*.tmp
*.bak
*.swp

# IDE
.vscode/
.idea/

# IaC state
*.tfstate
*.tfstate.*
.terraform/
.azd/

# Local env / secrets
.env
.env.local
*.publishsettings
scripts/parameters.json
scripts/users.csv
scripts/my-users.csv

# Deploy output (contains credentials — never commit)
scripts/out/

# Per-student infra (infra/ folder) — deploy output, build artifacts, local params, node deps
infra/out/
infra/**/parameters.json
infra/**/*.secret.*
infra/bicep/**/*.json
!infra/bicep/main.parameters.json
infra/web/node_modules/
